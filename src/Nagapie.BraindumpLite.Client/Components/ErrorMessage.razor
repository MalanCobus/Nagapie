@inject IStringLocalizer<AppResources> L
@if (!string.IsNullOrEmpty(Code)) { <div class="notice error" role="alert">@L["Error_" + Code]</div> }
@code { [Parameter] public string? Code { get; set; } }
